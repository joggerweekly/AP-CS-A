Unit 4: Iteration

Unit 5: Writing Classes

static method
example generated by google AI with "write a java person class with  static methods"
public class Person {
    private String name;
    private int age;
    private static int populationCount = 0;

    public Person(String name, int age) {
        this.name = name;
        this.age = age;
        Person.populationCount++;
    }

    public String getName() {
        return name;
    }

    public void setName(String name) {
        this.name = name;
    }

    public int getAge() {
        return age;
    }

    public void setAge(int age) {
        this.age = age;
    }

    public static int getPopulationCount() {
        return Person.populationCount;
    }

    public static Person createAdult(String name) {
        return new Person(name, 18);
    }

    public static boolean isAdult(int age) {
        return age >= 18;
    }

    public static void main(String[] args) {
        Person person1 = new Person("Alice", 25);
        Person person2 = new Person("Bob", 30);

        System.out.println(person1.getName() + " is an adult: " + Person.isAdult(person1.getAge()));
        System.out.println(person2.getName() + " is an adult: " + Person.isAdult(person2.getAge()));

        Person person3 = Person.createAdult("Charlie");
         System.out.println(person3.getName() + " is an adult: " + Person.isAdult(person3.getAge()));

        System.out.println("Total population: " + Person.getPopulationCount());
    }
}




Unit 10: Recursion
search and sort
https://runestone.academy/ns/books/published/csjava/Unit11-Recursion/topic-11-2-recursive-search-sort.html

/*
Welcome to JDoodle!

You can execute code here in 88 languages. Right now you’re in the Java IDE.

  1. Click the orange Execute button ▶ to execute the sample code below and see how it works.

  2. Want help writing or debugging code? Type a query into JDroid on the right hand side ---------------->

  3.Try the menu buttons on the left. Save your file, share code with friends and open saved projects.

Want to change languages? Try the search bar up the top.
*/

import java.util.Arrays;

public class Sort {
  public static void main(String args[]) {
    int x=10;
    int y=25;
    int z=x+y;

    System.out.println("Sum of x+y = " + z);
    
    
    int arr[] = { 12, 11, 13, 5, 6 };
  }

  public static void InsertionSortRecursive(int arr[]) {
    if (arr.length > 2)
    {
        //compare first element with the sorted sublist
        
        int[] sub = Arrays.copyOfRange(arr, 1, arr.length-1); 
        
        if (arr[0]>InsertionSortRecursive(sub)[0])
        {
            int t = arr[0];
            arr[0] = arr[1];
            arr[1] = t;
        }
        else
        {
            // add arr[0] to the front of the sorted list and return the new list
            
        }
    }
       //
    else
    {
        //swap if first element is bigger
        if (arr[0]>arr[1])
        {
            int t = arr[0];
            arr[0] = arr[1];
            arr[1] = t;
        }
        
    }
  }  
}

//practice exam provide by college board
https://plbailey79.github.io/portal/harc/z17/XCScix03_GridworldReleasedExam.pdf
https://apcentral.collegeboard.org/media/pdf/ap-computer-science-a-course-and-exam-description.pdf

//AP Computer Science A (2023) Review Questions
https://github.com/VerisimilitudeX/apcsa

Gridworld
student manual: 
https://secure-media.collegeboard.org/apc/GridWorld_Case_Study_Student_Manual_with_Appendixes_Aug_2007_updated.pdf

Solutions Document: 
https://secure-media.collegeboard.org/apc/ap07_gridworld_solutions_doc_v2.pdf

solution code:
https://github.com/parkr/GridWorld/
